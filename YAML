name: CI
on :
push:
branches: ( main ]
pull_request:
branches: [ main ]
jobs:
build:
runs-on: ubuntu-latest steps:
﻿﻿uses: actions/checkoutev3
﻿﻿name: Установка зависимостей
run: nom install
﻿﻿name: Запуск тестов
run: npm test
